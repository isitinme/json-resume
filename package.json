{
  "name": "json-resume",
  "type": "module",
  "version": "0.0.2",
  "description": "My resume in JSON format. It is generated by jsonresume and rendered by jsonresume-theme-even.",
  "main": "resume.json",
  "scripts": {
    "html": "resumed --theme ${THEME:-jsonresume-theme-even}",
	  "pdf": "node index.js",
    "validate": "resumed validate resume.json"
  },
  "dependencies": {
    "jsonresume-theme-even": "^0.22.0",
    "puppeteer": "^21.7.0",
    "resumed": "^3.0.1"
  },
  "devDependencies": {
    "prettier": "^3.2.1"
  }
}
